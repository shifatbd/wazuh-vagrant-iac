services:
  wazuh.dashboard:
    image: wazuh/wazuh-dashboard:${WAZUH_VERSION}
    hostname: wazuh.dashboard
    extra_hosts:
      - "wazuh-indexer.wazuh.internal:${WAZUH_INDEXER_IP}"
      - "wazuh-indexer:${WAZUH_INDEXER_IP}"
      - "wazuh-manager.wazuh.internal:${WAZUH_MANAGER_IP}"
      - "wazuh-manager:${WAZUH_MANAGER_IP}"
    restart: always
    ports:
      - "443:5601"
    environment:
      - INDEXER_USERNAME=admin
      - INDEXER_PASSWORD=vah1Shaibaa8voh
      - WAZUH_API_URL=https://wazuh-manager.wazuh.internal
      - DASHBOARD_USERNAME=kibanaserver
      - DASHBOARD_PASSWORD=kibanaserver
      - API_USERNAME=wazuh-wui
      - API_PASSWORD=MyS3cr37P450r.*-
    volumes:
      - ./config/certs/wazuh.dashboard.pem:/usr/share/wazuh-dashboard/certs/wazuh-dashboard.pem
      - ./config/certs/wazuh.dashboard-key.pem:/usr/share/wazuh-dashboard/certs/wazuh-dashboard-key.pem
      - ./config/certs/root-ca.pem:/usr/share/wazuh-dashboard/certs/root-ca.pem
      - ./config/wazuh_dashboard/opensearch_dashboards.yml:/usr/share/wazuh-dashboard/config/opensearch_dashboards.yml
      - /var/lib/wazuh-dashboard/custom:/usr/share/wazuh-dashboard/plugins/wazuh/public/assets/custom
